import { HomologacionPipe } from './homologacion.pipe';

describe('HomologacionPipe', () => {
  it('create an instance', () => {
    const pipe = new HomologacionPipe();
    expect(pipe).toBeTruthy();
  });
});
